{% extends 'base.html.twig' %}

{% block title %}Create new Member{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-header bg-white border-0 pt-4 pb-0">
                    <h1 class="h3 mb-0 text-primary">
                        <i class="fas fa-user-plus me-2"></i>Hi GymApp
                    </h1>
                    <p class="text-muted small mt-2">Create new Member</p>
                </div>
                <div class="card-body">
                    {{ include('member/_form.html.twig') }}
                    <div class="mt-4">
                        <a href="{{ path('app_member_index') }}" class="btn btn-outline-secondary btn-sm">
                            <i class="fas fa-arrow-left me-1"></i>back to list
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}